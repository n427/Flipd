import { useCallback, useEffect, useState } from 'react';
import { ActivityIndicator, Pressable, ScrollView, Text, View } from 'react-native';
import { Image } from 'expo-image';
import { Ionicons } from '@expo/vector-icons';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { SafeAreaView } from 'react-native-safe-area-context';
import { ScreenHeader } from '@/components/ScreenHeader';
import { Sheet, SheetGrabber } from '@/components/Sheet';
import { deleteWantedPost, fetchWantedOffersForPost, fetchWantedPost, resolveWantedOffer, WantedOffer, WantedPostDetail } from '@/lib/wanted';
import { wantedCardCopy } from '@/lib/wantedPresentation';
import { F, S, T } from '@/lib/theme';

export default function WantedDetail() {
  const { id } = useLocalSearchParams<{ id: string }>(); const router = useRouter(); const [detail,setDetail]=useState<WantedPostDetail|null>(null); const [myOffer,setMyOffer]=useState<WantedOffer|null>(null); const [state,setState]=useState<'loading'|'ready'|'error'>('loading'); const [confirm,setConfirm]=useState(false); const [error,setError]=useState('');
  const load=useCallback(async()=>{if(!id)return;try{const next=await fetchWantedPost(id);setDetail(next); if(!next.management){const offers=await fetchWantedOffersForPost(id).catch(()=>[]);setMyOffer(offers.find((offer)=>offer.role==='seller')??null);}setState('ready');}catch{setState('error');}},[id]); useEffect(()=>{load();},[load]);
  if(state==='loading')return <Center><ActivityIndicator color={T.cardinal}/></Center>; if(state==='error'||!detail)return <Center><Text style={muted}>Couldn’t load this request.</Text><Pressable onPress={load}><Text style={link}>Retry</Text></Pressable></Center>;
  const post=detail.wanted_post; const copy=wantedCardCopy(post); const owner=!!detail.management;
  const remove=async()=>{try{await deleteWantedPost(post.id);setConfirm(false);router.replace('/(tabs)/wanted');}catch(cause){setError(cause instanceof Error?cause.message:'Could not delete.');}};
  return <SafeAreaView style={{flex:1,backgroundColor:T.bg}} edges={['top']}><ScreenHeader right={owner&&post.status==='active'?<Pressable accessibilityLabel="Edit request" onPress={()=>router.push(`/wanted/${post.id}/edit`)}><Text style={link}>Edit</Text></Pressable>:undefined}/><ScrollView contentContainerStyle={{padding:S.gutter,paddingBottom:100}}>{post.photo_urls.length?<ScrollView horizontal showsHorizontalScrollIndicator={false} style={{marginHorizontal:-S.gutter}} contentContainerStyle={{gap:8,paddingHorizontal:S.gutter}}>{post.photo_urls.map((uri)=><Image key={uri} source={{uri}} style={{width:270,height:220,borderRadius:18}} contentFit="cover"/>)}</ScrollView>:null}<Text style={{fontFamily:F.black,fontSize:28,color:T.ink,marginTop:22,letterSpacing:-.6}}>{post.title}</Text><Text style={{fontFamily:F.extrabold,fontSize:18,color:T.cardinal,marginTop:7}}>{copy.budget}</Text><Text style={{fontFamily:F.medium,color:T.muted,marginTop:6}}>{post.category} · {post.location} · {copy.deadline}</Text><Text style={{fontFamily:F.regular,fontSize:16,lineHeight:24,color:T.ink,marginTop:22}}>{post.description}</Text>{detail.buyer?<View style={{marginTop:24,padding:14,borderWidth:1,borderColor:T.rule,borderRadius:14}}><Text style={{fontFamily:F.bold,color:T.ink}}>Requested by {detail.buyer.display_name??detail.buyer.handle??'a Flipd member'}</Text><Text style={muted}>Public profile and campus identity</Text></View>:null}{error?<Text style={{color:T.cardinal,fontFamily:F.medium,marginTop:12}}>{error}</Text>:null}{owner?<><Pressable onPress={()=>router.push('/(tabs)/requests?tab=wanted&direction=received')} style={primary}><Text style={primaryText}>Review offers ({post.offer_count})</Text></Pressable><Pressable onPress={()=>setConfirm(true)} style={secondary}><Text style={{fontFamily:F.bold,color:T.cardinal}}>Delete request</Text></Pressable></>:post.status==='active'?<>{myOffer?.status==='pending'?<><Pressable onPress={()=>router.push(`/wanted/${post.id}/offer?offerId=${myOffer.id}`)} style={primary}><Text style={primaryText}>Edit offer</Text></Pressable><Pressable onPress={async()=>{await resolveWantedOffer(myOffer.id,'withdraw');load();}} style={secondary}><Text style={{fontFamily:F.bold,color:T.cardinal}}>Withdraw offer</Text></Pressable></>:<Pressable onPress={()=>router.push(`/wanted/${post.id}/offer${myOffer?`?offerId=${myOffer.id}`:''}`)} style={primary}><Text style={primaryText}>{myOffer?'Send another offer':'Make an offer'}</Text></Pressable>}</>:<View style={secondary}><Text style={muted}>This request is closed.</Text></View>}</ScrollView><Sheet visible={confirm} onClose={()=>setConfirm(false)}><SheetGrabber/><Ionicons name="trash-outline" size={24} color={T.cardinal}/><Text style={{fontFamily:F.extrabold,fontSize:20,color:T.ink,marginTop:12}}>Delete this request?</Text><Text style={[muted,{marginTop:8,lineHeight:21}]}>Pending offers will close. If you already accepted an offer, that conversation stays available.</Text><Pressable onPress={remove} style={primary}><Text style={primaryText}>Delete request</Text></Pressable><Pressable onPress={()=>setConfirm(false)} style={secondary}><Text style={{fontFamily:F.bold,color:T.ink}}>Keep request</Text></Pressable></Sheet></SafeAreaView>;
}
function Center({children}:{children:React.ReactNode}){return <SafeAreaView style={{flex:1,backgroundColor:T.bg,alignItems:'center',justifyContent:'center',gap:12}}>{children}</SafeAreaView>} const muted={fontFamily:F.medium,color:T.muted,fontSize:13.5} as const; const link={fontFamily:F.bold,color:T.cardinal,fontSize:15} as const; const primary={backgroundColor:T.cardinal,borderRadius:13,paddingVertical:14,alignItems:'center',marginTop:24} as const; const primaryText={fontFamily:F.bold,color:'#fff'} as const; const secondary={borderWidth:1,borderColor:T.rule,borderRadius:13,paddingVertical:13,alignItems:'center',marginTop:10} as const;
