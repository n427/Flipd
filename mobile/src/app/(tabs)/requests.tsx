import { View, Text } from 'react-native';

export default function Requests() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#666' }}>Requests — coming soon</Text>
    </View>
  );
}
